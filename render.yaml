services:
  - type: web
    name: neuropredict-ai
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile.render
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
